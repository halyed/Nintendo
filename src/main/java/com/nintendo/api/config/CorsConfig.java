package com.nintendo.api.config;

// CORS is now handled by SecurityConfig
// This class is kept empty to avoid conflicts with Spring Security's CORS configuration

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
}
